services:
  - type: web
    name: school-accessibility-system
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: PORT
        value: 1111
      - key: NODE_ENV
        value: production
    healthCheckPath: /api/health
